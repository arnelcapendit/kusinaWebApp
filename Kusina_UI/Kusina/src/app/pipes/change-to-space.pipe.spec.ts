import { ChangeToSpacePipe } from './change-to-space.pipe';

describe('ChangeToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
