import { DataSource } from '@angular/cdk/collections';
import { Static } from 'highcharts/highstock';
import { AfterViewInit } from '@angular/core';

export class TableFilterModel implements AfterViewInit {

    constructor() {
        //console.log('test new');
    }

    refresh() {
    }

    ngAfterViewInit(): any {
    }
}
